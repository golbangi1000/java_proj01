package chapter01.ex01;

public class Package {

	/*
	 *  패키지 (package) 클래스 파일이 위치하는 폴더 
	 *  --- 패키지 명을 넣지 않으면 default package class파일이 저장 
	 *  --- 하위 패키지를 만들때는 . 을 사용해서 하위 패키지를 생성
	 *  --- 외부의 클래스 파일을 가져올때는 패키지로 분리해서 가져올수 있다
	 *  --- 패키지 생성, 클래스 생성, 클래스를 패키지 사이로 이동, 하위 패키지 생성
	 *  --- 기본적으로 JSP나 Spring을 구성할때 3단계의 Package가 기본적으로 등록 
	 *  ---com.naver.www : 클래스 저장
	 */
	public static void main(String[] args) {
		
		System.out.println("패키지 출력");
	}

}
